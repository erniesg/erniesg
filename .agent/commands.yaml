# Generated by rucksack. Commands are names, not secrets.
# build, test and e2e carry no per-lane timeout: scripts/agent-evidence runs
# them under one shared budget, DEFAULT_BUDGET_MS, and a per-lane cap here
# would restate or undercut it (see tools/agent-evidence-budget-rule.test.mjs).
version: 1
commands:
  install:
    command: "npm ci"
    timeout_seconds: 600
    required: true
  build:
    command: "npm run build"
    required: true
  test:
    command: "npm run test"
    required: true
  e2e:
    command: "npm run test:e2e"
    required: false
  dev:
    command: "npm run dev"
    timeout_seconds: 0
    required: false
  vm-verify:
    command: "infra/vm/verify.sh"
    timeout_seconds: 120
    required: false
