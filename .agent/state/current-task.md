# Current Task

- Issue/PR: #113 — Consolidate source-agnostic STRUCT extraction and typesetting
- Branch: codex/issue-113-struct-typeset
- Lane: trusted-vm (portable changes are prepared locally; VM queue is the overnight executor)
- Objective: Generalize PDF/DOCX extraction and rendering for unseen papers. Deterministically recover continuous prose, page furniture, figures/diagrams/captions, tables, equations, notes, citations, links, and preformatted blocks into STRUCT; prefer proven semantic tables and preserve every ambiguous bounded source region; render only from STRUCT; compare every rendition with its source before exposing human work.
- Last checkpoint: Table lineage, equation source-preservation, and actionable recovery are covered by deterministic tests. Full local evidence passed (build plus 2,460 tests, one skipped) and the Astro build passed. Ledger PR #117 merged and #113/#115 now have verified `main` provenance plus queue labels. The VM pulse guard is TDD-covered against systemd variable consumption, self-matching probes, any VM issue tmux worker, and direct Codex `exec` workers; the installed timer remains the durable scheduler. The default-branch `struct-typeset` skill is installed read-only on the VM at SHA-256 `3d96ae5ffb4a2c66a2cc6c393e4a1a89f0893dec2245998392636f0c13e6e37a`; future workers use `gpt-5.6-luna` / `max`. VM linger is enabled, so no active wait depends on the laptop. The latest completed branch evidence remains `.agent/evidence/20260801T081504739Z/manifest.json`. BookWorld remains the benchmark plus held-out corpus.
- Next action: The VM owns three serialized control loops. `erniesg-pr121-promote.service` may merge only exact green head `5aa4583` of bookkeeping PR #121, which gives issue #119 default-branch provenance for an event-driven fix to the load-sensitive PDF worker-cleanup test. `erniesg-issue-88-requeue.service` waits for the unrelated Rucksack #226 Codex worker to exit, then re-queues the exact preserved #88 provider result and restores `erniesg-struct-typeset-queue.timer` from its EXIT trap. `erniesg-issue-88-pr-hold.service` blocks the recovered #88 PR from overclaiming completion until a real pinned provider adapter, operator wiring, and BookWorld-plus-held-out benchmark exist. Do not delete or reimplement #88. #92 resumed without Codex, passed clean publisher evidence, and opened PR #118; that PR is merge-blocked on non-transitive source order, semantic-scope theft by generic preformatted detection, inexact indentation, and missing right-column-to-next-page-left continuation. After repair, continue the serialized 042 → 040 → 043 → 044 parser chain and disjoint evidence work. Fresh BookWorld evidence remains 0/8 semantic tables; after #88 lands, add the cell-scoped wrapped-lineage regression before claiming table recovery. Rucksack #347, #349, and #350, with ledger PR #348, track durable cross-pulse capacity, host-wide resource accounting, and evidence-only retries before model self-heal. Any model/layout consultation must remain candidate-constrained, provenance-recorded, and distilled into deterministic tests.
- Human decision needed: no (only ask after automatic source comparison identifies a specific page and exact action).
