# Current Task

- Issue/PR: #113 — Consolidate source-agnostic STRUCT extraction and typesetting
- Branch: codex/issue-113-struct-typeset
- Lane: trusted-vm (portable changes are prepared locally; VM queue is the overnight executor)
- Objective: Generalize PDF/DOCX extraction and rendering for unseen papers. Deterministically recover continuous prose, page furniture, figures/diagrams/captions, tables, equations, notes, citations, links, and preformatted blocks into STRUCT; prefer proven semantic tables and preserve every ambiguous bounded source region; render only from STRUCT; compare every rendition with its source before exposing human work.
- Last checkpoint: Table lineage, equation source-preservation, and actionable recovery are covered by deterministic tests. Full local evidence passed (build plus 2,460 tests, one skipped) and the Astro build passed. Ledger PRs #117, #121, and #122 merged; #113/#115/#119 now have default-branch provenance and queue state. The VM pulse guard is TDD-covered against systemd variable consumption, self-matching probes, any VM issue tmux worker, and direct Codex `exec` workers; the installed timer remains the durable scheduler. The default-branch `struct-typeset` skill is installed read-only on the VM at SHA-256 `3d96ae5ffb4a2c66a2cc6c393e4a1a89f0893dec2245998392636f0c13e6e37a`; the VM config was verified as the owner-requested `gpt-5.6-sol` / `high`. VM linger is enabled, so no queue wait depends on the laptop. The latest completed branch evidence remains `.agent/evidence/20260801T081504739Z/manifest.json`. BookWorld remains the benchmark plus held-out corpus.
- Next action: #119 is actively fixing the event-driven timeout harness on the trusted VM. Let its recorded `gpt-5.6-sol` / `high` run finish, then dispatch #115 for atomic checkpoints, safe disk high-water cleanup, and queue hardening, followed by #88 for the real pinned table provider plus BookWorld/held-out proof. The VM root disk was at 90% with 4.7 GiB free during the active run; preserve the live worktree and reclaim only checkpointed completed/expired worktrees or reproducible caches after it exits. Do not churn #92 labels while Rucksack #313/PR #315 owns its review-state oscillation. The persistent `erniesg-issue-88-pr-hold.service` blocks a partial #88 PR until provider wiring and real benchmark evidence exist. PR #118 remains merge-blocked on non-transitive source order, semantic-scope theft by generic preformatted detection, inexact indentation, and missing right-column-to-next-page-left continuation. After repair, continue the serialized 042 → 040 → 043 → 044 parser chain and disjoint evidence work. Fresh BookWorld evidence remains 0/8 semantic tables. #88 must preserve exact per-cell source-run lineage for shifted wrapped continuations/shared source lines and cover grouped headers plus row/column spans before claiming table recovery. Autonomous VM implementation stays `gpt-5.6-sol` / `high`; any bounded local layout consultation is a separate Codex task using `gpt-5.6-luna` / `max`, with its own provenance and deterministic distillation.
- Human decision needed: rotate and re-authenticate the trusted VM Claude Code OAuth credential because a failed custom watcher invocation wrote the inherited value to the private VM user journal. The Codex STRUCT queue does not depend on Claude auth and continues independently.
