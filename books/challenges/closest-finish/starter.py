"""The closest two ascents on the board.

    python3 books/tools/grade.py run closest-finish
"""


def closest_ascent(ascents: list[tuple[int, int]]) -> int:
    # 20 billion pairs will not finish. Put the times in order first.
    # Watch which part of each pair you are ordering by.
    raise NotImplementedError("Write me")
