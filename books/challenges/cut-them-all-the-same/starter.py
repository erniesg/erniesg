"""The longest lead they can cut.

    python3 books/tools/grade.py run cut-them-all-the-same
"""


def longest_lead(pieces: list[int], wanted: int) -> int:
    # There is no list to search here. Decide what the candidate answers are,
    # write the question "can the pile give me `wanted` leads this long?",
    # and check which way round the yeses run before you write the loop.
    raise NotImplementedError("Write me")
