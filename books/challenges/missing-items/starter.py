"""What still has to be bought.

    python3 books/tools/grade.py run missing-items
"""


def missing_items(requested: list[str], stocked: list[str]) -> list[str]:
    # Walk `requested` once, keeping the order.
    # Two questions per part: is it already in the bin, and is it already listed?
    # Both of those want a container that answers instantly.
    raise NotImplementedError("Write me")
