"""What the dips cost.

    python3 books/tools/grade.py run overdraft-fees
"""


def overdraft_cost(start: int, payments: list[int]) -> tuple[int, int]:
    # Set up the running balance, the lowest so far, and the total charged
    # BEFORE the loop. Then take each payment in order and ask two questions
    # about the new balance: is it a new low, and is it below zero?
    raise NotImplementedError("Write me")
