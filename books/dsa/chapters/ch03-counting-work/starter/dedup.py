"""Chapter 3 — Counting work: Big-O without the maths fog.

Implement `first_duplicate` in O(n) expected time.

    python3 books/dsa/tools/runner.py run ch03
"""


def first_duplicate(paths: list[str]) -> str | None:
    """Return the value whose second occurrence appears earliest, else None.

    Constraints: 0 <= len(paths) <= 5 * 10**5.
    The perf tier places the only duplicate at the very end of 500k items —
    an O(n^2) scan will blow the time limit.
    """
    # TODO: replace this with your implementation.
    return None
