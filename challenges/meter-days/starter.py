"""How long the credit lasts.

    python3 challenges/tools/grade.py run meter-days
"""


def days_of_credit(credit: int, usage: list[int]) -> int:
    # One trip through `usage` costs the same wherever it happens.
    # Answer the pattern-costs-nothing case first, then use that fact.
    raise NotImplementedError("Write me")
