"""The last few readings.

    python3 challenges/tools/grade.py run recent-readings
"""


def recent(readings: list[int], n: int) -> list[int]:
    # Hand back the last n readings, oldest first, as a list of their own.
    # Settle the empty screen before you slice: -0 is 0.
    raise NotImplementedError("Write me")
