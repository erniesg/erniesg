[Service]
# A full pass may include one bounded Codex worker plus source comparison.
# Avoid systemd's 90-second default oneshot timeout; the queue owns its
# bounded retries and provider/self-heal limits.
TimeoutStartSec=30min
TimeoutStopSec=5min
