/**
 * Private compatibility path retained for older local imports. Provider,
 * model, and PDF policy lives in `src/research/model-consultation-receipt`.
 */
export {
  validateStructConsultationReceipt,
  type StructConsultationReceipt,
} from './consultation-receipt'
