---
title: Payload parity fixture
description: A deterministic local Payload export used by the publication compiler.
date: 2026-01-01
authors:
  - Ada Lovelace
lang: en
translationKey: payload-equivalent
---

# Payload parity fixture

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

![A small geometric test card](./fixture-image.svg)

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.

Deterministic source adapters preserve authored meaning, stable identifiers, accessible media, and reproducible publication receipts across every supported output profile.
